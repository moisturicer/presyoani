{
  "name": "presyo-ani",
  "private": true,
  "version": "0.1.0",
  "description": "Buyer dashboard for PresyoAni",
  "workspaces": [
    "apps/buyer-dashboard",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=buyer-dashboard",
    "build": "turbo run build --filter=buyer-dashboard",
    "lint": "turbo run lint --filter=buyer-dashboard"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
